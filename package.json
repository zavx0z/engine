{
  "name": "@engine/workspace",
  "version": "0.0.1",
  "private": true,
  "description": "Built for MetaFor: reusable high-performance WebGPU infrastructure for immersive interfaces.",
  "homepage": "https://zavx0z.github.io/engine/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zavx0z/engine.git"
  },
  "metafor": "https://github.com/zavx0z/metafor",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bun run typecheck && bun run test && bun run --cwd packages/storybook build",
    "check:ci": "bun run typecheck && bun run test:ci && bun run --cwd packages/storybook build",
    "test": "bun test packages",
    "test:ci": "bun test packages --path-ignore-patterns='**/*.webgpu.test.ts'",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/storybook typecheck"
  },
  "devDependencies": {
    "@layout/core": "link:@layout/core",
    "@types/bun": "^1.4.0",
    "@ui/components": "link:@ui/components",
    "@ui/elements": "link:@ui/elements",
    "@webgpu/types": "^0.1.68",
    "@zavx0z/highlighter": "link:@zavx0z/highlighter",
    "@zavx0z/storybook": "link:@zavx0z/storybook",
    "bun-webgpu": "^0.1.6",
    "typescript": "^7.0.2"
  }
}
